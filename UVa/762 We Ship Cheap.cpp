#include<bits/stdc++.h>
using namespace std;
map < pair<string, string>, int> Map;
map < string, string>
void BFS(int source)
{
    Q.push(source);
    level[source] = 0;
    while(!Q.empty)
    {
        int u = Q.front();
        Q.pop;
        for(i=0; i<G[u].size(); i++)
        {
            int v = G[u][i];
            if(levle)
        }
    }
}

int main()
{

}
