#include<bits/stdc++.h>
using namespace std;
int main()
{
    int len,val;
    char str[1000],c;
    gets(str);
    //c = getchar;
    len = strlen(str);
    for(i = 0; i< len; i++)
    {

        if(str[i] <= 57 && str[i] <= 49)
        {
            val = str[i] - '0';
        }
        else if (str[i] =='!'){ printf("\n");}
        if(str[i+1] > 57 || str[i+1] < 49)
        {
            if(str[i]=='b')
        }
    }
}
