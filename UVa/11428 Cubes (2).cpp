#include <bits/stdc++.h>
using namespace std;
int main()
{
    double a,b,cubick;
    int i,j,n;
    while(scanf("%d",&n)==1)
    {
        if(n==0)
            break;
        a=0;
        b=0;
        for(i=n-1;i>0;i--)
        {
            cubick = pow();
            for(j = 1;j < )

        }
        if(a<b){i=a;a=b;b=i;}
        if(a==0 && b==0)
            printf("No solution\n");
        else { printf("%.0lf %.0lf\n",a,b);}
    }
    return 0;
}
